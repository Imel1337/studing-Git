# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил(а):
- Мельников Иван Александрович
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### Создайте две переменные, значение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется».

```python
one = int(input('Введите значение первой переменной: '))
two = int(input('Введите значение второй переменной: '))
if one >= two:
    print('Выполняется')
else:
    print('Не выполняется')
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_1.png)

## Выводы

Выполненные задания демонстрируют уверенное освоение базовых концепций программирования на Python:

1. Работа с типами данных - успешно применялись преобразования между типами `(int, float, str, bool)`, понимается разница между строковыми и числовыми значениями, включая особенности преобразования различных значений в булевый тип.

2. Математические операции - освоены основные арифметические операции, включая специальные (целочисленное деление, остаток от деления, возведение в степень), а также понимание особенностей работы с разными числовыми типами и приоритета операций.

4. Строковые операции - продемонстрировано владение различными методами работы со строками: срезы, конкатенация, умножение строк, методы `lower(), count(), replace()`, работа с escape-последовательностями и индексация.

5. Ввод-вывод данных - освоены функции `input()` и `print()`, включая форматированный вывод через f-строки, использование параметра end, а также обработка пользовательского ввода с преобразованием типов.

## Лабораторная работа №2
### Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else.

```python
one = int(input('Введите значение первой переменной: '))
if one < 0:
    print('Переменная меньше 0')
elif 0 < one < 10:
    print('Переменная больше 0 и меньше 10')
else:
    print('Переменная больше 10')
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_2.png)

## Выводы

Выполненные задания демонстрируют уверенное освоение базовых концепций программирования на Python:

1. Работа с типами данных - успешно применялись преобразования между типами (int, float, str, bool), понимается разница между строковыми и числовыми значениями, включая особенности преобразования различных значений в булевый тип.

2. Математические операции - освоены основные арифметические операции, включая специальные (целочисленное деление, остаток от деления, возведение в степень), а также понимание особенностей работы с разными числовыми типами и приоритета операций.

3. Строковые операции - продемонстрировано владение различными методами работы со строками: срезы, конкатенация, умножение строк, методы lower(), count(), replace(), работа с escape-последовательностями и индексация.



## Лабораторная работа №3
### Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.

```python
numbers = [1, 3, 4, 6, 8, 9]
value = int(input('Введите значение переменной: '))
if value in numbers:
    print('Переменная есть в данном массиве')
else:
    print('Переменной нет в данном массиве')
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_3.png)

## Выводы

Выполненные работы показывают, что базовые концепции Python не просто заучены, а осмысленно применяются на практике:

1. Гибкость работы с данными - вместо механического преобразования типов видно понимание, когда и зачем это нужно: от простого перевода строк в числа до тонкостей булевой логики, где пустая строка становится False, а любое число кроме нуля - True.

2. Осознанное использование операторов - математические операции применяются не по шаблону, а с пониманием их специфики: целочисленное деление для дискретных величин, обычное - когда важна точность, а возведение в степень и остаток от деления для специализированных расчетов.

3. Творческий подход к строкам - продемонстрировано, что строки в Python это не просто текст, а гибкий инструмент: умножение для генерации паттернов, срезы для точного выбора частей, методы для интеллектуальной обработки.
  
## Лабораторная работа №4
### Напишите программу, которая будет определять находится ли переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными значениями переменной value.

```python
numbers = [1, 3, 4, 6, 8, 9, 15, 16, 73, 321, 322]
value = int(input('Введите значение переменной: '))
if value in numbers:
    if value % 2 == 0:
        print('Переменная четная и есть в массиве numbers')
    else:
        print('Переменная нечетная и есть в массиве numbers')
else:
    print("Переменной нет в массиве numbers и она ровна {value}")
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_1.png)

## Выводы

1. Практическое применение вложенных условий - программа демонстрирует умение строить многоуровневую логику, где проверка принадлежности массиву является лишь первым фильтром, за которым следует анализ свойств элемента.

2. Глубокое понимание оператора in - видно, что освоена не только базовая проверка наличия элемента, но и интеграция этой проверки в более сложные логические конструкции.

3. Работа с четностью через остаток от деления - применен математический подход value % 2 == 0 вместо менее надежных методов, что показывает понимание математических основ программирования.

## Лабораторная работа №5
### Напишите программу, в которой циклом for значения переменной i будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.

```python
for i in range(10):
    print('i =', i)
    if i == 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i == 3:
        print('Переменная равна 2 или 3')
    elif i in [4, 5, 6]:
        print('Переменная равна 4, 5 или 6')
    else:
        break
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_5.png)

## Выводы

1. Нестандартное использование циклов - программа исследует границы поведения for-loop в Python, проверяя, как изменение переменной цикла влияет на его выполнение

2. Экспериментальный подход к range() - вместо шаблонного перебора демонстрируется попытка "обмануть" цикл через модификацию i, что показывает понимание разницы между переменной цикла и итератором

3. Тактическое применение управляющих конструкций - continue для пропуска итерации, break для досрочного выхода, создание сложной логики ветвления внутри цикла

## Лабораторная работа №6
### Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string. Определять индекс буквы не обязательно, но если вы хотите, то это делается при помощи строки: index = string.find(value) Вы берете название переменной, в которой вы хотите что-то найти, затем применяете встроенный метод find() и в нем указываете то, что вам нужно найти. Данная строка вернет индекс искомого объекта.

```python
string = 'Привет всем изучающим Python!'
value = input()
for i in string:
    if i == value:
        index = string.find(value)
        print(f"Буква '{value}' есть в строке под {index} индексом")
        break
else:
    print(f"Буквы '{value}' нет  указанной строке")
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_6.png)

## Выводы

1. Исследование малоизвестной особенности Python - программа демонстрирует понимание неочевидного синтаксиса else для циклов, который выполняется только если цикл не был прерван break

2. Практическая ценность подхода - такой паттерн исключает необходимость вводить дополнительные флаговые переменные для отслеживания успешности поиска

3. Эффективное сочетание методов - использование прямого перебора for i in string для проверки наличия символа с последующим точным определением позиции через find()

## Лабораторная работа №7
### Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы будет необходимо придумать свой вариант применения обратного цикла.

```python
value = 100 
for i in range(10, -1, -1):
    value -= i
    print(i, value)
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_7.png)

## Выводы

1. Освоение расширенного синтаксиса range() - программа демонстрирует понимание трехпараметрической версии range(start, stop, step) для создания обратной последовательности

2. Практическое применение обратного отсчета - вместо абстрактного перебора реализована конкретная математическая операция: последовательное вычитание значений от 10 до 0 из начальной суммы

3. Визуализация процесса вычислений - вывод пар значений (i, value) позволяет наблюдать динамику изменения переменной на каждом шаге, превращая код в наглядный эксперимент

## Лабораторная работа №8
### Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, поскольку циклы while при неправильно написанных условиях могут становится бесконечными, как указано в примере далее

```python
value = 0
while value < 100:
    if value == 0:
        value += 10
    elif value // 5 > 1:
        value *= 5
    else:
        value -= 5
    print(value)
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_8.png)

## Выводы

1. Осознанная работа с рискованными конструкциями - программа демонстрирует понимание потенциальной опасности while-loop, но использует его осмысленно с контролируемыми условиями

2. Динамическое изменение логики выполнения - внутри цикла реализовано ветвление, которое модифицирует поведение в зависимости от текущего состояния value, создавая нелинейный процесс

3. Исследование разных сценариев обновления - программа тестирует три различных стратегии изменения переменной: резкий скачок (+10), экспоненциальный рост (*5) и постепенное уменьшение (-5)

## Лабораторная работа №9
### Осознанная работа с рискованными конструкциями - программа демонстрирует понимание потенциальной опасности while-loop, но использует его осмысленно с контролируемыми условиями.

```python
value = 0
for i in range(10):
    for j in range(10):
        if i != j:
            value += j
        else:
            pass
print(value)
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_9.png)

## Выводы

1. Переход от рискованных к стабильным конструкциям - программа демонстрирует осознанный выбор for вместо while для задач с предсказуемым количеством итераций

2. Эффективное использование вложенных циклов - реализована сложная логика с двойной итерацией, где внешний и внутренний циклы работают согласованно

3. Оптимизация через отсечение избыточных операций - условие i != j исключает диагональные элементы, предотвращая ненужные вычисления когда индексы совпадают

## Лабораторная работа №10
### Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве, четных чисел.

```python
even_array = [2, 4, 6, 8, 9]
flag = False
for value in even_array:
    if value % 2 == 1:
        flag = True
        
if flag is True:
    print('В массиве есть нечетное число')
else:
    print('В массиве все числа четные')
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Lab3_10.png)

## Выводы 

Эта программа проверяет, есть ли в списке чисел хотя бы одно нечётное число.

1. Список чисел: [2, 4, 6, 8, 9] - в основном чётные числа, но есть одно нечётное (9)

2. Флаг-сигнализация: flag = False - изначально "сигнализация" выключена, значит нечётных чисел пока не нашли

3. Проверка каждого числа: берём каждое число из списка, проверяем: если число делится на 2 с остатком 1 (value % 2 == 1), значит оно нечётное, нашли нечётное число → включаем "сигнализацию" (flag = True)

## Самостоятельная работа №1
### Напишите программу, которая преобразует 1 в 31. Для выполнения поставленной задачи необходимо обязательно и только один раз использовать: • Цикл for • *= 5 • += 1 Никаких других действий или циклов использовать нельзя.

```python
x = 1
for i in range(2):
    x *= 5
    x += 1
print(x)
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Sam3_1.png)

## Выводы

В данном коде преобразуется число 1 в число 31 с использованием цикла for и математических операций:

1. x = 1: Создается переменная x с начальным значением 1.

2. `for i in range(2)`: Цикл выполняется ровно два раза, как и требуется по условию задачи.

3. x *= 5: На каждой итерации цикла значение переменной x умножается на 5. Это обязательная операция по условию задачи.

4. x += 1: После умножения к результату прибавляется 1. Это вторая обязательная операция по условию задачи.
  
## Самостоятельная работа №2
### Напишите программу, которая фразу «Hello World» выводит в обратном порядке, и каждая буква находится в одной строке консоли. При этом необходимо обязательно использовать любой цикл, а также программа должна занимать не более 3 строк в редакторе кода.

```python
text = "Hello World"
for char in reversed(text):
    print(char)
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Sam3_2.png)

## Выводы

В данном коде фраза «Hello World» выводится в обратном порядке, каждая буква на отдельной строке:

1. text = "Hello World": Создается строковая переменная text со значением "Hello World".

2. for char in reversed(text):: Используется цикл for для перебора символов строки text в обратном порядке. Функция reversed() возвращает итератор, который проходит строку от конца к началу.

3. print(char): На каждой итерации цикла выводится один символ из обращенной строки. Поскольку функция print() по умолчанию добавляет перенос строки, каждый символ оказывается на отдельной строке в консоли.
  
## Самостоятельная работа №3
### Напишите программу, на вход которой поступает значение из консоли, оно должно быть числовым и в диапазоне от 0 до 10 включительно (это необходимо учесть в программе). Если вводимое число не подходит по требованиям, то необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять в каком диапазоне находится полученное число. Нужно учитывать три диапазона: • от 0 до 3 включительно • от 3 до 6 • от 6 до 10 включительно Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строчек в редакторе кода.

```python
n = int(input())
if 0 <= n <= 10:
    if n <= 3:
        print("от 0 до 3 включительно")
    elif n <= 6:
        print("от 3 до 6")
    else:
        print("от 6 до 10 включительно")
else:
    print("Число не в диапазоне от 0 до 10")
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Sam3_3.png)

## Выводы

В данном коде обрабатывается числовой ввод пользователя и определяется диапазон значения:

1. n = int(input()): Программа получает ввод от пользователя и преобразует его в целое число. Если ввод не является числом, программа завершится с ошибкой.

2. if 0 <= n <= 10:: Проверяется, находится ли введенное число в допустимом диапазоне от 0 до 10 включительно.

3. Вложенные условия для определения диапазона:

-if n <= 3:: Если число меньше или равно 3, выводится сообщение "от 0 до 3 включительно"

-elif n <= 6:: Если число больше 3, но меньше или равно 6, выводится сообщение "от 3 до 6"

-else:: Если число больше 6, но не превышает 10, выводится сообщение "от 6 до 10 включительно"

-else:: Если число не попадает в диапазон от 0 до 10, выводится сообщение "Число не в диапазоне от 0 до 10" и программа завершает работу.
  
## Самостоятельная работа №4
### Манипулирование строками. Напишите программу на Python, которая принимает предложение (на английском) в качестве входных данных от пользователя. Выполните следующие операции и отобразите результаты: • Выведите длину предложения. • Переведите предложение в нижний регистр. • Подсчитайте количество гласных (a, e, i, o, u) в предложении. • Замените все слова "ugly" на "beauty". • Проверьте, начинается ли предложение с "The" и заканчивается ли на "end". Проверьте работу программы минимум на 3 предложениях, чтобы охватить проверку всех поставленных условий.

```python
sentence = input("Введите предложение на английском: ")

print("Длина предложения:", len(sentence))
print("В нижнем регистре:", sentence.lower())

kolvo = sum(1 for char in sentence.lower() if char in 'aeiou')
print("Количество гласных:", kolvo)

new_sentence = sentence.replace('ugly', 'beauty').replace('Ugly', 'Beauty')
print("После замены 'ugly' на 'beauty':", new_sentence)

starts_with_the = sentence.startswith('The')
ends_with_end = sentence.endswith('end')
print("Начинается с 'The':", starts_with_the)
print("Заканчивается на 'end':", ends_with_end)
print()
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Sam3_4.png)

## Выводы

В данном коде выполняются различные операции манипулирования со строкой, введенной пользователем:

1. sentence = input("Введите предложение на английском: "): Программа получает предложение от пользователя через консольный ввод.

2. print("Длина предложения:", len(sentence)): Выводится общее количество символов в предложении с помощью функции len().

3. print("В нижнем регистре:", sentence.lower()): Предложение преобразуется в нижний регистр с помощью метода lower().

4. kolvo = sum(1 for char in sentence.lower() if char in 'aeiou'): Подсчитывается количество гласных букв (a, e, i, o, u) с использованием генератора списка и функции sum().

5. new_sentence = sentence.replace('ugly', 'beauty').replace('Ugly', 'Beauty'): Заменяются все вхождения слова "ugly" (в любом регистре) на "beauty" с сохранением регистра.

6. starts_with_the = sentence.startswith('The'): Проверяется, начинается ли предложение с "The" с помощью метода startswith().

7. ends_with_end = sentence.endswith('end'): Проверяется, заканчивается ли предложение на "end" с помощью метода endswith().
  
## Самостоятельная работа №5
### Составьте программу, результатом которой будет данный вывод в консоль.

```python
string = 'hello'
counter = 0
values = [0, 2, 4, 6, 8, 10]
memory = 'world'

while counter != 10:
    if counter in values:
        memory = string
    if counter > 7:
        print(string + ' ' + memory)
    counter += 1
    if counter < 10:
        string = 'hello'
```
### Результат.
![Меню](https://github.com/Imel1337/studing-Git/blob/Tema_3/pic/Sam3_5.png)

## Выводы

В данном коде выполняется цикл while с различными условиями и модификациями переменных:

1. string = 'hello': Инициализация переменной string со значением 'hello'

2. counter = 0: Счетчик цикла, начинается с 0

3. values = [0, 2, 4, 6, 8, 10]: Список четных чисел от 0 до 10

4. memory = 'world': Переменная memory инициализируется значением 'world'

## Общие выводы по теме
Выполненные задания демонстрируют уверенное освоение базовых концепций программирования на Python:

-Условные конструкции - успешно применяются операторы if, elif, else для реализации сложной логики ветвления, включая проверку диапазонов значений, множественных условий и вложенных конструкций.

-Циклы - освоены различные типы циклов: for для работы с известным количеством итераций и коллекциями, while для ситуаций с неизвестным числом повторений, включая контроль условий выхода.

-Логические операторы - продемонстрировано понимание работы операторов сравнения (==, !=, <, >, <=, >=) и логических операторов (and, or, not, in) для построения сложных условий.

-Управление выполнением циклов - освоено использование break для досрочного прерывания циклов и continue для пропуска итераций, что позволяет эффективно управлять потоком выполнения.

-Работа с коллекциями в циклах - продемонстрировано умение использовать циклы для обработки списков, проверки наличия элементов и выполнения операций над данными.

-Паттерны программирования - освоены основные шаблоны работы с циклами, включая использование флагов для отслеживания состояния, накопление результатов и обработку граничных случаев.